[project]
name = "nous"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "optax>=0.2.4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
